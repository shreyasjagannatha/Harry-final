import { TimeminutePipe } from './timeminute.pipe';

describe('TimeminutePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeminutePipe();
    expect(pipe).toBeTruthy();
  });
});
